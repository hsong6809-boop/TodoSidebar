# TodoSidebar 代码审查报告

- **审查性质**：只读审查（未修改任何源文件）
- **审查日期**：2026-07-17
- **审查范围**：`Services/`、`Interfaces/`、`Models/`、`ViewModels/`、`Controls/`、`Converters/`、`Helpers/`、`Config/`、`Database/`，以及全部 `.xaml.cs`（App / MainWindow / FullWindow / LoginWindow / TaskDetailDialog / SettingsWindow / StatisticsWindow / UpgradeDialog）
- **方法**：静态代码通读 + 关键路径交叉核验（已对全部「重要」级问题逐条打开源文件确认）
- **目标框架**：net8.0-windows（WPF）

---

## 总体结论

项目可正常编译，数据库访问**全部使用参数化查询（未发现 SQL 注入）**，ViewModel 普遍正确实现 `INotifyPropertyChanged`，`SqliteConnection`/`SemaphoreSlim` 在 `DatabaseService.Dispose` 中释放。**但存在若干在生产环境会间歇触发、且会随使用时间累积的严重隐患**，集中在以下三类：

1. **并发安全**：单例 `SqliteConnection` 跨 UI 线程与后台同步线程未统一加锁。
2. **生命周期管理**：登出后旧 ViewModel、后台同步/通知循环、定时器未被释放 → 内存与 CPU 泄漏，且多次登出/登录后叠加。
3. **数据完整性**：备份还原「先删后导」无事务保护，中途失败会丢数据。

无「必然崩溃/编译失败」的致命缺陷，但下列「重要」问题在并发或异常路径下可造成数据损坏或持续泄漏，应优先修复。

---

## 问题统计

| 等级 | 数量 | 说明 |
|------|------|------|
| 重要 (High) | 6 | 并发、泄漏、异步、数据完整性 |
| 一般 (Medium) | 8 | MVVM 误用、线程、启动阻塞、架构不一致 |
| 建议 (Low) | 8 | 硬编码、死代码、魔法数字、日志缺失 |

> 标注 **[已核验]** 的问题为本次审查直接打开源文件确认；其余为全量扫描结果，建议二次确认。

---

## 一、重要问题（High）

### H1. 单例 SqliteConnection 跨线程并发访问，仅部分方法加锁  ⚠️[已核验]
- **位置**：`Services/DatabaseService.cs`
  - 单例唯一连接：`_connection`（第 35 行），`SemaphoreSlim _dbLock`（第 36 行）
  - 仅 `ExecuteLocked` 保护的约 3 个方法：第 595 / 614 / 652 行（`GetDirtyTasks` / `MarkTaskSynced` / `UpsertTaskFromRemote`）
  - 大量未加锁的公开方法：`InsertTask`、`UpdateTask`、`GetTasks`、`SearchTasks`、`GetTodayCompletedDailyTaskIds` 等，均直接 `_connection!.CreateCommand()` 执行
- **场景**：`SyncService` 用 `PeriodicTimer` 每 30 秒在**线程池线程**上跑同步循环（`GetDirtyTasks`/`UpsertTaskFromRemote` 已加锁），但 UI 线程上用户编辑任务触发的 `InsertTask`/`UpdateTask`/查询**未加锁**。
- **后果**：`Microsoft.Data.Sqlite` 的 `SqliteConnection` 非线程安全。编辑任务恰逢同步 tick 时，会抛 `database is locked`、读取错位甚至连接损坏。开启同步后正常使用下**必然间歇性出现**。
- **建议**：所有数据库访问统一经 `ExecuteLocked`；或每次操作使用独立连接（断开模式）；或把所有 DB 访问派发到单一专用线程。

### H2. 登出后旧 MainViewModel 及其 DispatcherTimer 永不释放（内存/CPU 泄漏）  ⚠️[已核验]
- **位置**：
  - 创建：`App.xaml.cs:71`（首次）、`LoginWindow.xaml.cs:132`（`App.SharedViewModel = new MainViewModel()`）
  - 释放：仅在 `App.OnExit` 调用 `SharedViewModel?.Dispose()`（`App.xaml.cs:195`）
  - 旧 VM 定时器：`ViewModels/MainViewModel.cs` 的 `_midnightTimer`（DispatcherTimer，触发时调用 `LoadData()`）
  - 登出入口：`MainWindow.xaml.cs:291`、`FullWindow.xaml.cs:62` → `AuthService.Instance.LogoutAsync()`，**未**调用 `SharedViewModel?.Dispose()` 也未置空
- **后果**：用户登出再登录时，`LoginWindow.xaml.cs:132` 直接覆盖 `SharedViewModel` 引用，旧 VM 的 `Dispose()` 从未被调用；旧 `_midnightTimer` 永久运行、反复查库，且通过 Tick lambda 持有旧 VM 引用使其无法被 GC 回收。多次登出/登录，泄漏叠加。
- **建议**：登出流程中显式 `App.SharedViewModel?.Dispose(); App.SharedViewModel = null;`；或让 VM 生命周期跟随窗口（`Window.Closed` 时 Dispose）。

### H3. 后台单例服务（Sync / Notification）登出后持续运行  ⚠️[已核验]
- **位置**：`Services/SyncService.cs`（`InitializeAsync` 第 93–122、`Stop` 第 468–478）、`Services/NotificationService.cs`（第 48–58）、登出入口未调用 `Stop()`
- **后果**：`SyncService` 同步循环与 `NotificationService` 检查定时器仅在 `App.OnExit` 停止。登出后仍在跑；且 `SyncService.InitializeAsync` 有 `if (_cts != null) return` 守卫，导致重新登录后旧循环仍在、新循环不会重启。登出后仍每分钟检查通知、每 30 秒尝试同步，造成资源浪费，并对已切换/清空的库做无意义查询。
- **建议**：登出时显式 `SyncService.Instance.Stop()` + `NotificationService.Instance.Stop()`；登录成功时按需重新 `InitializeAsync()`。

### H4. async void 登录状态事件处理器  ⚠️[已核验]
- **位置**：`App.xaml.cs:175–180`
  ```csharp
  _loginStateHandler = async (s, isLoggedIn) =>
  {
      if (isLoggedIn)
          await SyncService.Instance.InitializeAsync();
  };
  ```
- **后果**：订阅到 `EventHandler<bool>` 的 lambda 为 `async void`。若 `InitializeAsync()` 内部抛异常，异常在 UI 同步上下文上以「未处理异常」形式抛出；而 `App_DispatcherUnhandledException` 将 `e.Handled = true` 且只 `Debug.WriteLine`，异常被静默吞掉，登录后初始化失败难以排查。
- **建议**：改为 `async Task` 并在登录路径显式 `await`；或在处理器内 `try/catch` 并显式上报。

### H5. 还原备份「先全删后导入」，无事务保护（数据丢失风险）  ⚠️[已核验]
- **位置**：`Services/ExportService.cs:161–170`（`RestoreBackup`）
- **代码**：先遍历 `GetTasks()` 对每个任务 `DeleteTask(task.Id)`（软删除），再 `ImportFromJson(backupPath)` 逐条 `InsertTask`；整体被外层 `try/catch` 包裹向外抛。
- **后果**：若导入过程中任意一条插入失败并抛异常，**前面已执行的软删除不会回滚**，而新数据只部分写入 → 不可逆的部分数据丢失。
- **建议**：用**单个事务**包裹「删除 + 导入」，失败整体回滚；或先在临时库预演成功后再切换。

### H6. 数据库初始化失败后留下「空连接僵尸实例」  ⚠️[已核验]
- **位置**：`Services/DatabaseService.cs:15–32`（`Instance` getter）、`Initialize()` 第 47–158 行；`_connection!` 空宽恕操作符在全文件大量使用（第 203/226/261/272/286/300/327/379/388/404/418/432/448/467/479/493 行等）
- **后果**：`Instance` getter 先 `new DatabaseService()` 再 `Initialize()`；若 `Initialize` 抛 `InvalidOperationException`，`_instance` 已非 null 但 `_connection` 为 null。此后所有方法以 `_connection!` 访问 → 得到「僵尸」实例，访问即 `NullReferenceException`，而非有意义的错误。
- **建议**：初始化失败不应在赋值 `_instance` 之前就暴露实例；方法入口对 `_connection` 判空并抛明确异常，避免到处 `_connection!`。

---

## 二、一般问题（Medium）

### M1. RefreshTaskProperties 在错误对象上触发属性变更通知  ⚠️[已核验]
- **位置**：`ViewModels/MainViewModel.cs:359–365`
- **代码**：在 **MainViewModel** 上 `OnPropertyChanged(nameof(TaskItem.SubTasksList))` 等——这些是 `TaskItem` 的属性名。
- **后果**：UI 绑定绑定到 `TaskItem` 实例，MainViewModel 抛错名不会刷新界面。该方法是无效代码（真正刷新依赖随后的 `LoadCurrentTasks()` 重建集合）。
- **建议**：删除该方法，或改为在对应 `TaskItem` 实例上 `OnPropertyChanged`。

### M2. AnimationService 用 Int32Animation 动画 string 属性  ⚠️[已核验]
- **位置**：`Services/AnimationService.cs:136–143`（`AnimateNumberChange`）
- **代码**：对 `TextBlock.TextProperty`（`string` 依赖属性）使用 `Int32Animation`（`int`）。
- **后果**：类型不匹配，数字变化动画不生效（静默失败），且可能与绑定产生冲突。
- **建议**：改用 `DoubleAnimation` + `StringFormat`/转换器，或自写 `StringAnimationBase`，或直接更新绑定源。

### M3. TagControl 的 Tags 依赖属性使用可变的共享默认值  ⚠️[已核验]
- **位置**：`Controls/TagControl.cs:13–15`
- **代码**：`new PropertyMetadata(new ObservableCollection<string>())` 提供引用类型可变默认值。
- **后果**：所有未显式设置 `Tags` 的 `TagControl` 实例会**共享同一个集合**；在一个控件上增删标签会同时影响其它未赋值的实例（经典 WPF 默认共享 bug）。
- **建议**：默认值用 `null`，在 `OnApplyTemplate`/属性变更回调中 Lazy 创建；或始终由调用方提供集合。

### M4. NetworkMonitor 在网络回调线程上触发事件（跨线程隐患）  🔍[扫描]
- **位置**：`Services/NetworkMonitor.cs:35–66`，`SyncService.cs:113–121` 订阅
- **后果**：`NetworkChange` 回调在线程池线程，`UpdateStatus` 直接 `ConnectivityChanged?.Invoke`。当前订阅者未直接碰 UI，但任何未来订阅者若在其中更新 UI 而不用 `Dispatcher` 会崩溃。
- **建议**：`NetworkMonitor` 内部用 `Application.Current.Dispatcher` 封送，或文档明确约定订阅者自行封送。

### M5. 启动时用 `.GetAwaiter().GetResult()` 阻塞 UI 线程  🔍[扫描]
- **位置**：`App.xaml.cs:53–56`
- **后果**：`OnStartup` 中 `Task.Run(async () => { await InitializeAuthAsync(); }).GetAwaiter().GetResult();` 在主线程阻塞；`InitializeAuthAsync` 可能走网络（Supabase 会话恢复）。网络慢/不可达时应用白屏、主窗口迟迟不显示。
- **建议**：改为异步驱动（显示 Splash/启动窗口，完成后再 `Show` 主窗口），避免阻塞 UI 线程。

### M6. SupabaseClientService.InitializeAsync 实质为空操作 + 双重检查锁无 volatile  🔍[扫描]
- **位置**：`Services/SupabaseClientService.cs:45–59`
- **后果**：同步创建 client 后仅 `await Task.CompletedTask`；双重检查锁依赖非 `volatile` 的 `_client`，弱内存模型下存在极小可见性风险；方法名暗示异步初始化，实际是同步副作用。
- **建议**：同步即可则去掉 `InitializeAsync` 包装；或将 `_client` 声明为 `volatile` / `Volatile.Read`，改用 `Lazy<T>`。

### M7. 时间处理本地/UTC 混用（同步增量与显示可能偏差）  🔍[扫描]
- **位置**：`SyncService.cs:194`（`DateTime.Now`）与 `:197`（`DateTime.UtcNow`）；`DatabaseService.MarkTaskSynced:626`（`DateTime.UtcNow`）与 `ReadTask:371`（`DateTime.Parse`，Kind 不明确）；`TaskService.CompleteTask:84`（`DateTime.Now`）
- **后果**：同一数据在写入/读取、本地/云端、显示之间混用本地与 UTC 时间，且 `DateTime.Parse` 得到的 Kind 不明确。非 UTC 时区机器上，同步增量判断（`UpdatedAt >= _lastSyncTimeUtc`）可能漏/重复同步，统计与显示也可能偏差数小时。
- **建议**：持久化与网络传输统一用 UTC（`DateTime.UtcNow` + `DateTimeKind.Utc`），仅在 UI 展示时转本地。

### M8. 窗口/服务普遍直接 new 单例，绕过 DI 容器  🔍[扫描]
- **位置**：`SettingsWindow.cs:18`、`StatisticsWindow.cs:16`、`NotificationService`/`TaskService` 各处
- **后果**：`App.ConfigureServices` 已注册 DI，但大量代码直接 `new` 单例与依赖；`StatisticsWindow` 与 `MainViewModel` 各自持有独立的 `StatisticsViewModel` 实例，造成架构不一致、重复实例，多份独立 VM 还可能数据视图不一致。
- **建议**：统一通过 `App.Services.GetRequiredService<T>()` 获取，或复用 `MainViewModel.StatisticsViewModel`。

---

## 三、建议（Low）

1. **L1 硬编码 Supabase URL / Anon Key** — `Config/SupabaseConfig.cs:17–18`。anon key 本就公开，但注释写「不要提交真实 key」却硬编码真实值；`Reload()` 会覆盖静态字段，运行时改配置行为不易预期。建议源码仅留占位，完全依赖环境变量/配置文件。
2. **L2 DataProtectionHelper 回退明文** — `Services/DataProtectionHelper.cs:31–37`。`Protect` 失败时返回明文密码，可能落库到 `Settings.SavedPassword`。建议失败时抛出或至少告警并阻止保存凭据。
3. **L3 死代码 using** — `Services/SyncService.cs:7` `using Newtonsoft.Json;` 全文未使用（实际用 `System.Text.Json`）。删除。
4. **L4 异常普遍被 Debug.WriteLine 吞掉，缺乏持久日志** — `AuthService`/`SyncService`/`DatabaseService`/`TaskService` 等多处 `catch { Debug.WriteLine(...) }`。建议引入 `ILogger` 统一记录到文件，便于现场诊断。
5. **L5 魔法数字散布** — `SyncIntervalSeconds=30`、试用 14 天、重试 500/5000ms、窗口高度 `0.66`、硬编码颜色值等。建议提取为具名常量/配置。
6. **L6 快捷键热键未实现功能** — `App.xaml.cs:115–123`，`NewTaskRequested`/`SearchRequested` 仅 `mainWindow?.Activate()`，未真正打开新建/搜索界面。建议补全或移除。
7. **L7 转换器命名误导** — `Converters/Converters.cs:77–92`，名为 `BoolToAnimationConverter` 却返回 `Visibility`。建议重命名为 `BoolToVisibilityConverter`。
8. **L8 MainViewModel.Dispose 重复 Stop** — `ViewModels/MainViewModel.cs:426–427` `_midnightTimer?.Stop();` 写了两次。无害，属复制粘贴瑕疵，删除重复行。

---

## 四、已确认无问题项（值得肯定的部分）

- **SQL 注入 / 数据注入**：✅ 未发现。所有查询（含 `SearchTasks` 的 `LIKE '%'+参数+'%'`、`MigrateDatabase` 动态列名来自硬编码字典、`PurgeDeletedTasks` 的 `datetime('now','-'+参数+' days')`）均参数化或使用受控常量，无字符串拼接用户输入。
- **空引用判空（数据库层）**：✅ `ReadTask` 对各可空列都做了 `IsDBNull` 判断；`App.SharedViewModel` 在登录路径中先赋值再使用窗口，无 NPE（NPE 风险仅在 H6 初始化失败后才出现）。
- **资源释放（连接层）**：✅ `SqliteConnection`/`SemaphoreSlim` 在 `DatabaseService.Dispose` 中释放；无完全空的 `catch {}`（grep 验证）。
- **MVVM 基础**：✅ ViewModel 普遍实现 `INotifyPropertyChanged`（CommunityToolkit `ObservableObject` / `[ObservableProperty]`），绑定路径基本正确。

---

## 五、修复优先级建议

| 优先级 | 问题 | 投入 |
|--------|------|------|
| P0（尽快） | H1 并发锁统一、H5 还原备份加事务、H6 僵尸实例 | 中 |
| P1（本迭代） | H2/H3 登出释放 VM 与后台服务、H4 async void | 中 |
| P2（下迭代） | M3 TagControl 共享默认、M1 无效通知、M2 动画类型、M5 启动阻塞、M7 时区统一、M8 DI | 中 |
| P3（清理） | L1–L8 各项 | 小 |

> 注：本报告为只读审查产物，**未对工程内任何源文件做修改**。如需我协助修复，请另行告知，我会在 Craft 模式下逐项落地。
