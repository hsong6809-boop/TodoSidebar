# TodoSidebar v4.1.0 代码审查报告

- **审查基线**：commit `d02e946`（v4.1.0: 架构精简 + 侧边栏悬浮修复 + 性能优化）
- **审查日期**：2026-08-04
- **审查方式**：全文只读审查（50 个源文件，约 10,354 行）+ 关键结论二次核验
- **未修改任何源文件**

---

## 一、问题总览

| 严重级 | 数量 | 说明 |
|--------|------|------|
| **P0（必须修复）** | 6 | 安全漏洞 ×2、并发锁缺失 ×1、生命周期泄漏 ×2、商业化闸门失效 ×1 |
| **P1（应当修复）** | 17 | 事务/数据一致性、异步异常、资源释放、线程安全等 |
| **P2（建议清理）** | 20+ | 代码质量、重复资源、魔法数字、日志缺失 |

---

## 二、P0 必须修复

### P0-1 硬编码 Supabase 凭据进源码仓库【安全】
- **位置**：`Config/SupabaseConfig.cs:17-18`
- **问题**：真实的 Supabase URL 和 Anon Key（JWT，含 `exp`）直接硬编码为默认值，且已随 `git commit` 进入仓库。任何人 clone 即可提取；若云表 RLS 策略配置遗漏（`Database/init.sql` 中 `sync_conflicts` 表仅有 SELECT/INSERT 策略），匿名访问可能越权读取数据。
- **建议**：删除硬编码默认值，仅从环境变量 / `AppData\TodoSidebar\supabase.json` 读取；缺失配置时启动即报错（fail-fast）而非回退默认值；config 文件加入 `.gitignore` 并考虑轮换当前已泄露的 Anon Key。

### P0-2 DPAPI 加密失败静默回退明文，登录凭据可明文落盘【安全】
- **位置**：`Services/DataProtectionHelper.cs:31-37`（Protect 失败返回明文）、`:54-64`（Unprotect 失败返回原文）、`Services/AuthService.cs:246-248`（`IsProtected(raw) ? Unprotect(raw) : raw` 兼容明文读取）
- **问题**：三条通道叠加：① 加密抛异常时 `Protect` 返回 `plainText`，`AuthService.SaveSessionToFile`（:223-224）将 AccessToken+RefreshToken **明文写入 session.json**；② 解密失败 `Unprotect` 返回密文原文；③ `IsProtected` 靠"尝试解密"判定，失败即当作明文继续解析。实际效果是凭据可能以明文落盘且没有任何告警。
- **建议**：加密失败视为致命错误（记录日志 + 不落盘 + 提示用户），绝不回退明文；解密失败返回 null 并触发重新登录；用固定 Magic 前缀（如 `DPAPI:`）标记密文，替代双重解密探测。

### P0-3 数据库并发锁只覆盖同步方法，单例连接存在并发写风险【并发】
- **位置**：`Services/DatabaseService.cs:35`（进程唯一 `SqliteConnection`）、`:730-742`（`ExecuteLocked`）
- **问题**：只有 `GetDirtyTasks` / `MarkTaskSynced` / `UpsertTaskFromRemote` 3 个方法走 `_dbLock`（SemaphoreSlim），其余 20+ 个方法（`InsertTask:201`、`UpdateTask:224`、`DeleteTask:258`、`GetTasks:297`、`UpdateTaskOrder:566`、`GetSetting:377`、全部 DailyTaskCompletion 方法等）**直接操作 `_connection` 不加锁**。而 UI 线程（任务操作、`NotificationService.CheckNotifications` 每 60s 读库）与后台同步线程（`SyncService` 的 `PeriodicTimer` 循环 + 网络恢复 handler 写库）交叉访问同一连接。`Microsoft.Data.Sqlite` 连接不支持多线程并发命令，可导致 "database is locked"、`ObjectDisposedException` 或数据错乱。
- **建议**：所有公开 DB 方法统一经 `ExecuteLocked` 串行化；或改用连接池（每次打开短连接）。

### P0-4 登出/登录切换不释放旧 ViewModel 与后台定时器【生命周期】
- **位置**：`LoginWindow.xaml.cs:132`（登录成功直接 `App.SharedViewModel = new MainViewModel()` 覆盖旧实例，未 Dispose）；`App.xaml.cs:188-199`（仅在进程退出 `OnExit` 才 `SharedViewModel?.Dispose()`）
- **问题**：旧 `MainViewModel` 持有 `_midnightTimer`（跨午夜 DispatcherTimer）、SyncViewModel 订阅等，登出后不销毁 → 定时器继续在 UI 线程跑 DB 查询；反复登出/登录累积泄漏；停留登录界面时 `NotificationService` 定时器仍在运行。
- **建议**：登出路径统一执行 `App.SharedViewModel?.Dispose(); App.SharedViewModel = null;` + `NotificationService.Instance.Stop()`；登录替换前先 Dispose 旧实例。

### P0-5 async void 登录事件处理器无 try/catch【异常处理】
- **位置**：`App.xaml.cs:175-179`
- **问题**：`_loginStateHandler = async (s, isLoggedIn) => { ... await SyncService.Instance.InitializeAsync(); }` 是 async void lambda，无内部 try/catch。`InitializeAsync` 抛出的异常逃逸后被全局 `DispatcherUnhandledException` 接住并静默吞掉（`e.Handled = true`）。
- **建议**：lambda 内包 try/catch 并记录日志；或改为显式注册 `async Task` 方法。

### P0-6 许可证/功能开关闸门形同虚设【商业化】
- **位置**：`Services/LicenseService.cs:42-50`（`ValidateLicense` 对非 Free 层级无条件 `return true`）、`Services/FeatureFlagService.cs:40-42`（所有功能恒 true，注入的 `_licenseService` 从未使用）
- **问题**：接口注释承诺"Free 用户禁用 Pro 功能"（`IFeatureFlagService.cs:6`），实现却全量放行；`ValidateLicense` 不校验到期时间/签名/Key。一旦任何路径把 `_currentTier` 置为 Pro 即完全绕过闸门。
- **建议**：`ValidateLicense` 对非 Free 必须真实校验（服务端签名/到期时间），支付后端接入前非 Free 分支应返回 false；`FeatureFlagService` 先按 `_licenseService.IsPro` 实现真实判定。

---

## 三、P1 应当修复

| # | 位置 | 问题 |
|---|------|------|
| P1-1 | `App.xaml.cs:53-56` | 启动路径 `Task.Run(...).GetAwaiter().GetResult()` 同步阻塞，启动期间 UI 冻结；应改为异步启动或延迟初始化 |
| P1-2 | `App.xaml.cs:154-168` | 全局异常兜底仅 `Debug.WriteLine`，Release 构建下**生产异常完全静默**；应落盘日志（`%LOCALAPPDATA%\TodoSidebar\logs`） |
| P1-3 | `Services/SyncService.cs:468-478` | `Stop()` 后 `_cts` 未置 null、`_syncLoopTask` 未 await 清理，再次 `InitializeAsync` 会因 `_cts != null` 直接 return，同步无法重启 |
| P1-4 | `Services/SyncService.cs:242-331, 336-446` | `UploadLocalChangesAsync`/`DownloadRemoteChangesAsync` 顶层 catch 吞异常返回 0，`SyncAsync`（:178-216）仍置 `Success = true`，**同步失败误报成功** |
| P1-5 | `Services/DatabaseService.cs:284-295` | `PurgeDeletedTasks` 用 `datetime('now',...)` 与 `LastSyncedAt` 存的 ISO-8601 "O" 格式做字符串比较，格式不一致导致清理逻辑失效/误删 |
| P1-6 | `Models/TaskItem.cs:55`、`Services/SyncService.cs:194,197,391`、`Services/AuthService.cs:252` | 时间 UTC/本地混用：任务 CreatedAt 存本地时间、同步标记用 UtcNow、冲突判断 `remoteTask.UpdatedAt > existing.LastSyncedAt` 时区错位、session 过期用 `UtcNow` 比较库返回的本地 `ExpiresAt()`（8 小时偏差） |
| P1-7 | `Services/ExportService.cs:161-170` | `RestoreBackup` 先软删全部任务再导入，**无事务**；备份损坏/导入中途失败则本地任务全删且 `IsDirty=1`，下次同步把删除推到云端 |
| P1-8 | `Services/ExportService.cs:112-119` + `Services/DatabaseService.cs:204-208` | 导入 `task.Id=0` 且 `InsertTask` 不写 SyncId，导入副本以新 UUID 上传 → 云端产生重复任务 |
| P1-9 | `Services/AuthService.cs:200-232` | token 自动刷新（`AutoRefreshToken=true`）后新 token 不回写 session 文件，重启后读旧 refresh token 失败 → 用户被静默登出 |
| P1-10 | `Controls/TagControl.cs:13-15` | `DependencyProperty` 默认值共享同一个 `ObservableCollection`，未显式绑定的实例互相影响 |
| P1-11 | `Controls/CircularProgress.cs:10-28` | `Size/StrokeThickness/ProgressBrush` 等 DP 未声明 `AffectsRender`，属性变化不重绘；`StrokeThickness > Size` 时半径可为负 |
| P1-12 | `Services/NetworkMonitor.cs:14, 65` | ① `ConnectivityChanged` 在系统线程触发，UI 订阅方需自行封送；② `Instance` 用 `??=` 非线程安全，首访并发可创建双实例并双重订阅 |
| P1-13 | `Services/HotkeyService.cs:38-59` | 重复 `RegisterHotkeys` 未先注销，第二次注册失败但旧热键仍存活，`_isRegistered` 状态与实际不符；注册失败仅 Debug 输出 |
| P1-14 | `Services/NotificationService.cs:14-15, 48-58, 183-192` | 两个 DispatcherTimer 仅 Stop 不 Dispose、无 IDisposable；通知窗 closeTimer 在点击关闭路径不清理；`_notifiedTasks`（HashSet）跨线程无同步 |
| P1-15 | `FullWindow.xaml.cs:37`、`SettingsWindow.cs:64`、`UpgradeDialog.xaml.cs:52`、`TaskDetailDialog.xaml.cs:191` | 4 处 `DragMove()` 无 try/catch（MainWindow 有），鼠标未按下时抛 `InvalidOperationException` |
| P1-16 | `ViewModels/MainViewModel.cs:105-116` | 午夜定时器每次重建未 Dispose 旧实例；`msUntilMidnight` 无下限保护，时钟调整时 `Interval` 可为负抛异常 |
| P1-17 | `StatisticsWindow.cs:209-220` | catch 块内再次执行同样的 `ColorConverter.ConvertFromString(color)`，非法颜色时二次抛异常导致 UI 初始化崩溃 |

---

## 四、P2 建议清理（节选）

**异常处理 / 日志**
- `MainWindow.xaml.cs:160-166, 259, 409-412`：多处空 catch 无日志。
- `StatisticsWindow.cs:32-35, 54-58, 114-118, 163-166, 215-220, 233-236`：6 处 `FindResource` 失败空 catch。
- `ThemeManager.cs:62-73`：注册表读取空 catch。
- `TaskTemplateService.cs:67`：空 catch 静默；`:25` `GetTemplates` 直接返回内部集合引用，外部可绕过 `SaveTemplates` 修改。

**绑定 / 通知**
- `ViewModels/MainViewModel.cs:355-357`：`RefreshTaskProperties` 在 MainViewModel 实例上触发 `TaskItem` 属性的 `OnPropertyChanged`，子任务进度/折叠 UI 收不到通知（被后续 `LoadData` 兜底掩盖）。
- `StatisticsViewModel.cs:92-93, 110-111`：`ContainsKey` + 索引双重访问字典，应 `TryGetValue`。

**颜色 / 资源**
- `Converters/Converters.cs:19-21, 140-146`：Converter 颜色（255,90,90 等）与 `TaskItem.PriorityColor`（`#EF4444` 等）不一致，同一优先级不同位置颜色不同；每次 Convert 都 new Brush 未 Freeze。
- `App.xaml:42-67`：同色 Brush 以多个 key 重复定义（Accent/Primary/TypeDaily 同色等 4 组）。
- `App.xaml` + 各窗口：硬编码十六进制色值未资源化；`LoginWindow.xaml` 通篇硬编码（`#5B5FE9` 等），完全不随主题切换。
- `App.xaml:138-176, 389-496`：`SuccessButton`/`DangerButton`、`TaskCardTemplate`/`DeadlineTaskCardTemplate` 模板几乎逐行重复，应 `BasedOn` 合并。

**性能 / XAML**
- `MainWindow.xaml:55-57, 588-590`：MainPanel 常驻 `DropShadowEffect`（BlurRadius=15）+ 透明窗口，软件渲染下开销大；3px 宽的 TriggerStrip 也挂阴影，纯开销。
- `MainWindow.xaml:301,327`：DataTrigger 用魔法数字 `Value="1"` 比较枚举，应改 `Value="Deadline"`。

**其它**
- `App.xaml.cs:88-123`：`NewTaskRequested`/`SearchRequested` 两个 handler 完全相同，可合并。
- `MainWindow.xaml.cs:782-785`：`SWP_NOMOVE`/`SWP_NOSIZE` 未使用常量。
- `FullWindow.xaml.cs:30-33`：`Window_Loaded` 空方法体死代码。
- `UpgradeDialog.xaml.cs:67-75`：支付/激活按钮仅占位文案，建议置灰。
- `MainWindow.xaml.cs:191,154`：魔法数字（30、0.66）。
- `MessageService.cs:18-45`：`Dispatcher.Invoke` 同步阻塞，建议 `InvokeAsync`。
- `AuthService/TaskTemplateService/SyncLogService/ExportService`：`File.WriteAllText` 非原子写，建议"临时文件 + `File.Replace`"。
- `Services/LicenseService.cs:38`：Debug 输出泄露 licenseKey 前 8 位。
- `Interfaces/IExportService.cs`：`GetBackupList`/`CreateBackup` 未在接口声明，按接口编程拿不到备份列表。
- `Services/SyncLogService.cs:14`：`SyncLogEntry.Timestamp = DateTime.Now` 本地时间，与同步日志的 UTC 时区混用。
- `Services/SupabaseClientService.cs:77-81`：`Dispose` 无锁置空，与 `Client` getter 竞态；内部 HttpClient 未真正释放。
- `Services/AnimationService.cs:194-236`：粒子动画被打断时 `Completed` 不触发，粒子残留在 Canvas；`:73-82` 每次 `AnimateComplete` 新建 DispatcherTimer。
- `Models/SyncModels.cs`：`SyncTask` 缺 `estimated_minutes`/`actual_minutes` 映射，与本地模型不一致。
- `Services/FeatureFlagService.cs`：8 个预定义属性与 `ProFeatures` 集合重复维护两份 key 字符串。

---

## 五、已验证的亮点（未发现问题）

| 维度 | 结论 |
|------|------|
| **SQL 注入** | ✅ 未发现。本地查询全部参数化，`Database/init.sql` 无动态拼接，RLS 策略四类齐全 |
| **弱加密算法** | ✅ 未发现 DES/MD5/固定 IV；DPAPI（AES-256 内核）+ 固定 Entropy（唯一问题是 P0-2 的回退明文） |
| **绑定 Path 拼写** | ✅ 全部 XAML 绑定路径逐一核对存在，未发现拼写错误 |
| **async void 滥用** | ✅ 除 App.xaml.cs:175 登录事件（P0-5）外未发现其它裸 async void |
| **Finalizer / 析构函数** | ✅ 未发现 |
| **死锁（.Result 同步上下文）** | ✅ 未发现实际死锁；唯一同步阻塞点是启动路径（P1-1，功能性问题非死锁） |
| **P/Invoke 签名** | ✅ `HotkeyService`、`BlurHelper` 的 Win32 声明与 user32.dll/dwmapi.dll 一致 |
| **后台线程直接访问 UI** | ✅ 未发现（跨线程点均在服务层事件，UI 订阅方多数已封送或风险被记录） |

---

## 六、修复优先级建议（Top 8）

1. **P0-1 / P0-2**：清理硬编码凭据 + 禁止加密回退明文（安全底线）
2. **P0-3**：DatabaseService 全方法加锁（并发稳定性）
3. **P0-4 / P0-5**：统一 ViewModel 生命周期 + async void 补 try/catch（泄漏与异常逃逸）
4. **P1-4**：同步失败不应误报 Success（数据可信度）
5. **P1-7 / P1-8**：恢复备份事务化 + 导入透传 SyncId（防数据丢失与云端重复）
6. **P1-6**：时间统一 UTC（同步一致性）
7. **P1-2**：全局异常落盘日志（可观测性）
8. **P1-10 / P1-11**：控件 DP 修复（TagControl 共享集合、CircularProgress 重绘）

---

*审查为只读完成，未修改任何文件。如需按本报告逐项修复，可另行安排。*

---

## 七、修复记录（2026-08-04 实施）

本次按本报告完成一轮修复，**Release 全量编译 0 警告 0 错误，32/32 单元测试通过**。

### 已修复（P0 全部 6 项）

| # | 问题 | 修复内容 |
|---|------|----------|
| P0-1 | 硬编码 Supabase 凭据 | `SupabaseConfig.cs` 移除默认 URL/Key，改为仅环境变量 + AppData/supabase.json，缺失即 fail-fast；`EnsureLoaded` 加锁；`SyncIntervalSeconds` 改只读。**注意：需配置环境变量或 supabase.json 才能联网，并建议到 Supabase 控制台轮换旧 Anon Key** |
| P0-2 | DPAPI 明文回退 | `DataProtectionHelper` 加 `DPAPI:` 前缀；Protect 失败抛异常不落盘；Unprotect 失败返回 null；IsProtected 拒绝明文；`AuthService.LoadSessionFromFile` 不再兼容明文；`LoginWindow` 解密失败置空 |
| P0-3 | 数据库并发锁 | `DatabaseService` 全部 20+ 公开方法统一经 `ExecuteLocked` 串行化；`GetTaskBySyncId` 拆内部无锁实现避免重入死锁；`UpdateTaskOrder` 事务挂锁 |
| P0-4 | 登出/登录泄漏 | `App.StopBackgroundServices()`（Stop 同步/通知 + Dispose SharedViewModel）；`LoginWindow` 登录前 Dispose 旧 VM；`MainWindow`/`FullWindow` 登出路径调用清理；`MainViewModel.Dispose` 去重并置空 timer |
| P0-5 | async void 登录事件 | `App.xaml.cs` 登录状态 handler 包 try/catch |
| P0-6 | License/FeatureFlag 闸门 | `LicenseService.ValidateLicense` 非 Free 一律拒绝（许可服务接入前）；`FeatureFlagService` 保持全量开放策略（业务代码未消费 flag、单元测试断言 Free 全开），注释契约如实更新 |

### 已修复（P1 全部 17 项）

- **P1-1** 启动 `Task.Run+GetResult` 阻塞 → `async void OnStartup` + await，不再卡 UI
- **P1-2** 全局异常仅 Debug → 新增 `LogError` 落盘 `%APPDATA%\TodoSidebar\logs\app.log`，UI 线程 + AppDomain 两个 handler 均记录
- **P1-3** `SyncService.Stop` 清理 `_cts/_syncTimer/_syncLoopTask` 并置 null + 复位防重入，支持重新初始化
- **P1-4** 同步失败误报成功 → `UploadLocalChangesAsync`（全部失败抛异常）/`DownloadRemoteChangesAsync`（异常 rethrow）让 `SyncAsync` 正确置失败
- **P1-5** `PurgeDeletedTasks` 时间格式 bug → 改为 C# 计算同格式 cutoff 字符串比较
- **P1-6** 时间 UTC 统一 → `ReadTask` 全部改用 `DateTimeStyles.RoundtripKind` 解析（UTC/本地语义保留）；下载任务 `LastSyncedAt = UtcNow`；session 过期比较 `ToUniversalTime()`
- **P1-7** `RestoreBackup` 非事务 → 新增 `DatabaseService.ReplaceAllTasks`（单事务软删+插入，失败回滚）；备份解析失败不触碰现有数据
- **P1-8** 导入丢 SyncId → 新增 `InsertImportedTask`（保留 SyncId/IsDirty/IsDeleted），导入/恢复不再产生云端重复
- **P1-9** token 刷新不回写 → 订阅 `Auth.AuthStateChanged`，`TokenRefreshed` 时用 `CurrentSession` 回写 session 文件
- **P1-10** TagControl 共享集合 → DP 默认值改 null，getter 按需创建独立集合
- **P1-11** CircularProgress 缺重绘 → 全部 DP 加 `AffectsRender`；Progress 钳制 [0,1]（绑定路径也钳制）；radius 负值保护
- **P1-12** NetworkMonitor → `Lazy` 线程安全单例 + `volatile` 状态
- **P1-13** HotkeyService → `RegisterHotkeys` 开头强制 `UnregisterHotkeys`，杜绝重复注册叠加
- **P1-14** NotificationService → Lazy 单例、`_notifiedTasks` 加锁、午夜清空改按日期判断、通知窗点击关闭时停止 closeTimer
- **P1-15** 4 处 `DragMove` 无保护 → 全部 try/catch + 日志（FullWindow/SettingsWindow/UpgradeDialog/TaskDetailDialog）
- **P1-16** 午夜定时器反复重建 → 复用单 timer + `Math.Max(1000,...)` 下限
- **P1-17** StatisticsWindow catch 二次抛异常 → 新增 `TryParseColor` 先解析，失败回退默认色

### 已清理（P2 主要项）

空 catch 补日志（MainWindow×3、StatisticsWindow×6、ThemeManager、TaskTemplateService）；`RefreshTaskProperties` 改为在 TaskItem 上触发（`OnPropertyChanged` 改 public + `NotifySubTaskPropertiesChanged`）；Converters 颜色统一为 `Palette` 静态 Freeze 刷子（与 TaskItem/App.xaml 一致）；StatisticsViewModel 改 `TryGetValue`；MessageService 改 `InvokeAsync`；文件原子写（session/模板/同步日志/JSON 导出）；`GetTemplates` 返回副本；`SupabaseClientService.Dispose` 加锁；`LicenseService` 移除 key 泄露输出；`IExportService` 补 `GetBackupList`；删除未用常量/死代码/重复 handler；`UpgradeDialog` 统一走 DI；AuthService 恢复 session 增加 token 空值防护；修复 4 个编译警告。

### 跳过项（说明理由）

- **XAML 资源重构**（App.xaml 重复 Brush/模板、LoginWindow 硬编码色值资源化、阴影性能）：纯重构，改动面大、不影响功能，风险与收益不匹配，留待 UI 专项
- **SyncModels 补 `estimated_minutes`/`actual_minutes` 列**：涉及数据库表结构 + 同步映射双端改动，建议配合 Supabase 表结构变更一起做
- **AnimationService 粒子动画中断泄漏**：低概率场景，改动风险高
- **MainWindow failSafeTimer 每次展开新建**：轻微资源问题，窗口短生命周期内影响可忽略
- **StatisticsWindow 每次 new 全量查询**：窗口为模态短生命周期，可接受

### 修复后验证

- `dotnet build -c Release --no-incremental`：**0 警告 0 错误**
- `dotnet test`（TodoSidebar.Tests）：**32/32 通过**（其中 2 个 DataProtectionHelper 测试按新安全语义更新断言）

> ⚠️ **部署提醒**：P0-1 移除硬编码凭据后，本机运行需配置 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 环境变量，或创建 `%APPDATA%\TodoSidebar\supabase.json`（`{"Url": "...", "AnonKey": "..."}`）；并建议轮换已泄露的 Anon Key。
