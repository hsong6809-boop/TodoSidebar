# TodoSidebar v4.2.2 全面代码审查报告

- **审查基线**：commit `cb11a7c`（v4.2.2：升级系统 P1-P5 全部完成）
- **审查日期**：2026-08-21
- **审查性质**：只读检查（未修改任何源文件）
- **审查范围**：全部 68 个源文件（约 13,000 行），覆盖 Services / Interfaces / Models / ViewModels / Controls / Converters / Helpers / Config / Database / 全部 `.xaml(.cs)` 窗口 / 两个 Supabase SQL 脚本 / 测试工程
- **审查方法**：6 个模块并行深度审查（数据层、同步认证、成长系统、ViewModel 层、侧边栏 UI、完整模式窗口）+ 横切面专项检查（async/线程/异常处理/日期一致性）+ 高危发现逐条人工复核源码验证
- **编译验证**：`dotnet build TodoSidebar.sln` 通过，0 错误

---

## 一、总体结论

项目整体架构清晰、参数化查询规范（未发现 SQL 注入面）、密码凭据使用 DPAPI 加密、云端表启用 RLS，基础安全意识良好。**但本轮审查在"数据安全"和"云同步正确性"两条主线上发现了成簇的严重缺陷**：

1. **本地数据安全**：数据库打开失败即删库重建、备份恢复会孤儿化子表、登出不清本地库导致跨账号数据泄露；
2. **云同步基本失效或有害**：最常见的"带登录态重启"路径下自动同步从未启动；冲突解决基线错误可在弱网下静默丢失用户编辑；切换账号后同步游标共享导致云端任务大面积不下载；
3. **游戏化经济可被无限刷**：番茄每日目标奖励重复发放、取消完成任务不回收 XP 可跨天刷级、错过午夜连击永久漏结算；
4. **核心 UI 功能失效**：拖拽排序完全无效、子任务删除按钮删错条目、复选框勾选永远弹回、125%+ 缩放屏上侧边栏位置尺寸错乱。

无"必然崩溃/无法编译"级致命缺陷，但 **17 个严重问题中有 9 个直接威胁用户数据（丢失/泄露/污染）**，建议优先修复数据与同步主线后再迭代功能。

---

## 二、问题总览

| 严重度 | 数量 | 说明 |
|---|---|---|
| 🔴 严重 | 17 | 数据丢失/泄露/核心功能失效，应立即修复 |
| 🟠 中等 | 36 | 特定场景下的功能错误、性能隐患、体验缺陷 |
| 🟡 轻微 | 30 | 代码质量、边界健壮性、观感问题 |
| 合计 | **83** | 另有测试覆盖缺口清单见第七节 |

---

## 三、严重问题（P0，共 17 项）

### A. 本地数据安全（3 项）

**S1. 数据库打开失败即删库重建，瞬时错误也触发毁坏性重建**
- 位置：`Services/DatabaseService.cs:73-95`
- 问题：try 块只包含 Open + WAL 设置，任何异常——包括文件被其他进程短暂占用、磁盘满、杀软锁定等可恢复错误——都会走 `File.Delete(_dbPath)` 删掉用户全部数据。备份仅一份滚动 `.corrupted.bak`（每次覆盖），且不含 WAL 中未 checkpoint 的近期事务，删除时也未清理残留的 `todo.db-wal/-shm`。
```csharp
catch (Exception ex) {
    ...
    File.Copy(_dbPath, backupPath, overwrite: true);
    File.Delete(_dbPath);
```
- 建议：仅对真正的损坏错误（SqliteException 且 SqliteErrorCode 为 NotADatabase/Corrupt）才重建；重建前将 `.db/-wal/-shm` 一并复制为带时间戳的多份备份；其余异常直接向上抛出。
- 复核状态：✅ 已人工核实

**S2. 僵尸单例 + Dispose 顺序错误**
- 位置：`Services/DatabaseService.cs:27-28, 1371-1375`
- 问题：① `Initialize()` 抛异常时 `_instance` 已缓存、`_connection` 为 null，后续所有调用在 `_connection!` 处 NRE 而非报有意义错误；② `Dispose()` 先释放 `_dbLock` 再释放连接，此时仍在运行的定时器线程若阻塞在 `_dbLock.Wait()` 会抛 `ObjectDisposedException`，且 `_instance` 不重置。
- 建议：Initialize 成功后再发布 `_instance`；Dispose 先连接后信号量并置空 `_instance`；退出流程先停后台服务再 Dispose。

**S3. 恢复备份用新自增 Id 重建任务，子表引用全部孤儿化**
- 位置：`Services/ExportService.cs:179` + `Services/DatabaseService.cs:876-882`
- 问题：恢复备份时 `task.Id = 0` 让 SQLite 重新分配 Id，但 `DailyTaskCompletion.TaskId`、`XpLog.TaskId`、`PomodoroSession.TaskId` 仍指向旧 Id。恢复后每日任务的"今日已完成"状态丢失/错位，连击、XP 统计、番茄统计引用到不存在或错误的任务。
- 建议：按 SyncId 匹配复用原 Id；否则在同一事务内按旧→新 Id 映射批量 UPDATE 三张子表。

### B. 云同步正确性（5 项）

**S4. 启动时恢复会话成功后，同步服务永远不会启动**
- 位置：`App.xaml.cs:193-206` + `Services/AuthService.cs:93`
- 问题：`AuthService.InitializeAsync()` 内部恢复会话成功时已同步触发 `LoginStateChanged(true)`，而 App 的处理器在其后才订阅：
```csharp
await AuthService.Instance.InitializeAsync();   // :193 事件在此期间已发出
...
AuthService.Instance.LoginStateChanged += _loginStateHandler;   // :206 订阅太晚
```
- 影响：带有效 session.json 重启应用（日常最常见路径）时，`SyncService.InitializeAsync()` 的唯一调用点（App.xaml.cs:199）永不执行 → 30 秒定时同步、网络恢复同步、增量游标加载、成长数据同步全部静默失效，只有手动点按钮才同步。
- 建议：订阅 handler 后再检查一次 `IsLoggedIn`，为真则直接初始化同步服务。
- 复核状态：✅ 已人工核实（全项目唯一调用点）

**S5. user_profile 每次上传都生成新 GUID 主键，upsert 退化为无限插入**
- 位置：`Services/SyncService.cs:522-557` + `Models/SyncModels.cs:124-125` + `supabase_setup.sql:70-80`
- 问题：模型 `Id { get; set; } = Guid.NewGuid();` 每次 new 都是全新主键，Upsert 按 PK 冲突解析必然 INSERT；`user_profile` 表对 `user_id` 无唯一约束；下次合并 `FirstOrDefault()` 无 OrderBy 任意取一行。
- 影响：云端档案表随同步次数无限膨胀；两台设备 TotalXp/等级来回跳动，"大者胜"合并策略失效。
- 建议：上传前复用远程行 Id（或按 user_id 冲突列 upsert）；数据库加 `unique index on user_profile(user_id)`；查询加 OrderBy 取最新。
- 复核状态：✅ 已人工独立发现并交叉确认

**S6. 登出不清本地库，账号切换后前一用户的脏任务被上传到新账号名下**
- 位置：`Services/AuthService.cs:190-205` + `Services/SyncService.cs:273`
- 问题：`LogoutAsync()` 只 SignOut + 删 session 文件，不动 SQLite；上传时无条件按当前用户打标 `UserId = userId`。
- 影响：用户 A 登出时有 IsDirty=1 残留，用户 B 同机登录后 30 秒内，A 的任务内容被上传进 B 的云账号；B 的界面同时显示 A 的全部本地任务——隐私泄露。
- 建议：登出清空本地业务表（或本地数据按 owner_user_id 隔离）；上传前校验归属。

**S7. LWW 冲突比较基线错误，弱网下较新的本地修改会被较旧的云端版本永久覆盖**
- 位置：`Services/SyncService.cs:413-416`
- 问题：
```csharp
if (remoteTask.UpdatedAt > existing.LastSyncedAt)
    _dbService.UpsertTaskFromRemote(localTask);   // 整行覆盖 + IsDirty=0
```
比较的是"远程修改时间 vs 本地上次同步时间"，而 TaskItem 根本没有本地编辑时间戳字段。正常流程"上传先行"掩盖了缺陷；一旦批量上传部分失败（`:334` 仅全部失败才抛出），脏任务进入此分支后几乎恒判"远程胜"，本地更新的编辑被旧云端版本覆盖且 IsDirty 清零。另 `MarkTaskSynced`（DatabaseService.cs:756-764）无条件清 IsDirty，与上传期间的用户编辑也存在竞态窗口。
- 影响：弱网/token 过期等常见场景下静默永久丢失用户编辑，无任何提示。
- 建议：TaskItem 增加 LocalUpdatedAt 做真正 LWW；远程胜前将本地版本存入快照表供恢复；部分上传失败时中止本轮下载覆盖。
- 复核状态：✅ 已人工独立发现并交叉确认

**S8. 增量同步游标全局共享，切换账号后云端任务大面积不下载**
- 位置：`Services/SyncService.cs:99-103, 365`
- 问题：`GetSetting("LastSyncTimeUtc")` 无用户维度。A 登出、B 登录后，B 的首次同步沿用 A 的游标做增量过滤 → B 大部分历史任务不被拉取，界面近乎空白，用户感知等同"数据丢了"。
- 建议：游标键改为 `LastSyncTimeUtc:{userId}`；检测到用户变化时置空强制全量。

### C. 游戏化经济（2 项）

**S9. 连击结算只在午夜定时器触发，错过午夜则永久漏结算**
- 位置：`Services/LevelService.cs:148`（唯一业务调用点 `MainViewModel.OnMidnightRollover`）
- 问题：内部用 `DateTime.Today.AddDays(-1)` 判定"昨天"。应用在 00:00 未运行（最常见场景）时该天永不结算——昨天全清白清，今天没清反而被误断连。
- 建议：UserProfile 增加 `LastComboSettledDate`，启动与午夜触发时从上次结算日循环补结算到昨天（幂等由 XpLog 日期兜底）。

**S10. "每日目标"奖励对第 5、6、7…个番茄重复发放**
- 位置：`Services/PomodoroService.cs:207`
- 问题：
```csharp
if (completedToday >= DailyTarget)
    LevelService.Instance.Reward("pomodoro_daily", 10, null);
```
`taskId=null` 使 LevelService 的防重检查（`if (taskId.HasValue && ...)`）完全失效，`>=` 判定使每天第 4 个之后的每个番茄都额外 +10 XP，经验经济持续膨胀。
- 建议：改为 `== DailyTarget`，或让 Reward 对 null-taskId 来源支持按 `(source, date)` 幂等。
- 复核状态：✅ 已人工核实

### D. 核心 UI 功能失效（7 项）

**S11. 拖拽排序完全失效：SortOrder 写入数据库但显示路径不按它排序**
- 位置：`Services/TaskService.cs:46-49`（配合 `MainWindow.xaml.cs:839`、`MainViewModel.cs:496-520`）
- 问题：侧边栏主列表绑定 `CurrentTasks`（MainWindow.xaml:335），其数据源 `GetCurrentTasks()` 在内存中 `OrderBy(t => t.Type).ThenBy(t => t.Deadline)` 重排，丢弃 SortOrder；拖放写完 SortOrder 后紧接着重查，顺序立即被打回。且 `UpdateTaskOrder` 不置 `IsDirty=1`，排序永不同步到云端。
- 建议：统一 `ORDER BY SortOrder, CreatedAt DESC`；UpdateTaskOrder 加 `IsDirty = 1`。
- 复核状态：✅ 已人工核实全链路

**S12. 「今日完成率」口径错误，历史完成的截止任务全算进今天**
- 位置：`ViewModels/StatisticsViewModel.cs:74-95`（同时展示于 StatisticsWindow 与 FullWindow 统计页）
- 问题：`deadlineCompleted` 统计的是**所有历史** `IsCompleted=true` 的截止任务，未按 CompletedAt 过滤今天；分母同样是全部存量任务。
- 影响：半年内完成过 40 个截止任务、今天只完成 2 个的用户看到"今日完成 42"；与 MainViewModel.LoadTodayCompletedTasks（正确按日期过滤）互相矛盾。
- 建议：截止任务的今日口径改用 `GetCompletedTasks(today, tomorrow)` 过滤；分母只统计"今日应做"。

**S13. 子任务删除按钮删错条目，且错误结果被持久化**
- 位置：`TaskDetailDialog.xaml:136,165` + `TaskDetailDialog.xaml.cs:111-122`
- 问题：`AlternationCount="2"` 使 AlternationIndex 只在 0/1 循环，删除按钮却拿它当集合索引用 `_subTasks.RemoveAt(index)`。子任务超过 2 条时，点第 3、5、7…条的 × 删掉的是第 1 条，点第 4、6…条删掉第 2 条。
- 建议：改为 `Tag="{Binding}"` 传 SubTask 对象后 `_subTasks.Remove(subTask)`。
- 复核状态：✅ 已人工核实

**S14. 子任务复选框双重取反，勾选永远弹回原状**
- 位置：`TaskDetailDialog.xaml:150` + `TaskDetailDialog.xaml.cs:76-85`
- 问题：`IsChecked="{Binding IsCompleted}"`（TwoWay 绑定）+ Click 处理器内再手动 `subTask.IsCompleted = !subTask.IsCompleted;`。WPF 的 Click 在翻转并回写绑定之后才触发，处理器把状态又翻回去，净效果为零。
- 建议：删除处理器中的手动取反，只保留 `_hasChanges = true; UpdateProgress();`。
- 复核状态：✅ 已人工核实代码链路（建议运行确认一次）

**S15. MainWindow / FullWindow 订阅单例事件从不退订，窗口反复创建导致泄漏与重复横幅**
- 位置：`MainWindow.xaml.cs:52-59`、`FullWindow.xaml.cs:32-44`
- 问题：两窗口构造时订阅 `PomodoroService.Instance.Tick`、`vm.LevelUpOccurred` 等应用级单例事件，均无 OnClosed 退订。侧边栏⇄完整模式每次切换都 `new` 新窗口，旧窗口整棵视觉树被单例引用无法回收；番茄钟运行时幽灵窗口每秒照常更新，升级横幅触发 N 次。
- 建议：OnClosed 中对称退订全部事件；窗口切换尽量复用实例；彻底方案为弱事件。

**S16. Window_Loaded 把物理像素当 WPF DIP 使用，缩放屏上窗口位置/尺寸全错**
- 位置：`MainWindow.xaml.cs:343-354`
- 问题：`System.Windows.Forms.Screen.Bounds` 返回物理像素，直接赋给 WPF 的 Left/Top/Width/Height（DIP）；随后 SetWindowPos 又把这些 DIP 值当物理像素传入——双重错配。125%/150% 缩放（Windows 笔记本默认）下窗口横向越出屏幕、高度占满整屏而非设定的 66%。
- 建议：用 `CompositionTarget.TransformToDevice` 换算或改用 `SystemParameters.WorkArea`；删除多余的 SetWindowPos。
- 复核状态：✅ 已人工核实

**S17. 未完成子任务文字用近白色，浅色主题下不可见**
- 位置：`Converters.cs:26,267`（使用处 `MainWindow.xaml:472`）
- 问题：转换器返回冻结的硬编码 `#DCDCE6`（近白），而应用默认浅色主题卡片背景近白，对比度约 1.2:1，未完成子任务文字几乎看不见；完全绕过主题系统。
- 建议：改为查 `Application.Current.Resources["TextBrush"]` 或 XAML DataTrigger 替代该转换器。

---

## 四、中等问题（P1，共 36 项）

### 数据层（6 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M1 | DatabaseService.cs:783,800,822 | GetTaskBySyncId 不过滤软删除，UPDATE 按 SyncId 波及所有重复行，LWW 判断读到错误基准 | 查询加 `AND IsDeleted=0`；为 SyncId 建唯一部分索引 |
| M2 | DatabaseService.cs:304 vs 829-831 | Tasks 时间列混合"本地偏移"与"UTC(Z)"两种文本格式，字符串排序/范围比较失真 | 入库前统一 ToUniversalTime() |
| M3 | DatabaseService.cs:280-283,1265-1268 | 迁移 ALTER 失败被静默吞掉，之后每次 AddXpLog 抛异常，表现为"完成任务不发经验"难排查故障 | 改用 PRAGMA table_info 检测列，缺失 fail-fast |
| M4 | ExportService.cs:116-123 | ImportFromJson 无事务逐条插入（半量导入无回滚），且不按 SyncId 去重（重复导入整套翻倍） | 单事务包裹 + SyncId 查重 |
| M5 | ExportService.cs:58,69-70 | CSV 导出无 BOM，Excel 打开中文全乱码；非原子写，中途崩溃留截断文件 | UTF8Encoding(true) + temp+Move |
| M6 | ExportService.cs:86-97 | CSV 公式注入未中和（`=cmd|...` 类标题在 Excel 中被执行） | 以 `=+-@` 开头的字段前置 `'` |

### 同步与认证（7 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M7 | AuthService.cs:97-101 | SetSession 抛异常即删 session 文件——开机瞬间离线也被永久登出 | 仅 invalid_grant 类错误才删；网络异常保留重试 |
| M8 | AuthService.cs:291-296 | access token 过期（1 小时）预检直接删文件，不尝试仍有效的 refresh token | 删除预检，让 SetSession 自行刷新 |
| M9 | AuthService.cs:255-266 | session 保存用固定 .tmp 名并发踩踏；任何写盘异常都删主文件 → 偶发莫名登出 | tmp 加随机后缀；仅加密失败才删 |
| M10 | SyncService.cs:285,361-366 | 上传强制 UpdatedAt=UtcNow 且游标在下载后才推进 → 刚上传的行每轮被"回声下载"重写；游标依赖各设备本地时钟 | 服务端 trigger 维护 updated_at；游标取服务器返回最大值 |
| M11 | SyncService.cs:269 | `Guid.Parse(task.SyncId)` 无保护，一条损坏 SyncId 让整个上传永久卡死 | 改 TryParse，失败视为无 SyncId |
| M12 | SyncService.cs:364-373 | 下载无分页/Limit 校验，超服务端单页上限（默认 1000 行，待验证）时静默截断 | 分页循环拉取 + 总数校验 |
| M13 | supabase_tasks_rls.sql:20 | tasks 表建表语句不在仓库，RLS 脚本全新环境报错中断；tasks 若无 RLS 即公开可读写（anon key 曾进 git 历史） | 补齐幂等建表语句置于最前 |

### 游戏化（6 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M14 | LevelService.cs:131-132 等 | XP 结算"查重→写档案→写流水"三步非原子（Reward/挑战/连击同模式），崩溃窗口可重复发奖或丢经验 | DatabaseService 提供单事务 RewardAtomic |
| M15 | PomodoroService.cs:177 | 专注时长按墙钟计算，暂停时间计入 → ActualMinutes 与专注统计虚高 | 用 TotalSeconds - RemainingSeconds 推导 |
| M16 | PomodoroService.cs:131-137 | Stop(true) 无最短专注校验，开始后立刻"完成"即可得 XP，并可批量刷成就与挑战进度 | complete=true 时校验已专注时长阈值 |
| M17 | PomodoroService.cs:198-199 | 绑定任务的番茄经验被"同任务同天"去重，第 2+ 个番茄零经验，与未绑定路径矛盾 | 防重键加入会话维度或去掉 taskId |
| M18 | DailyChallengeService.cs:125 | `date.GetHashCode()` 是进程级随机化哈希，重启/多设备生成的当日挑战不一致 | 用稳定种子（如日期字符串的确定性哈希） |
| M19 | LevelService.cs:157-163 | 连击判定用"今天"的每日任务总数判定"昨天"是否全清，今天增删任务会追溯改写历史连击 | 增加 AsOf(date) 快照查询 |

### ViewModel 与统计（5 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M20 | DatabaseService.cs:381-388 | 软删除不清理 DailyTaskCompletion，孤儿记录虚高 streak/完成数，且会把昨天追溯"判为全清"续命连击 | 统计查询 JOIN 过滤 IsDeleted，或软删时清理 |
| M21 | TaskService.cs:138-162 | 取消完成不回收 XP + 防重按天失效 → 截止任务"完成→恢复→明天再完成"循环刷级 | Refund API 回滚流水，或防重改为任务生命周期一次 |
| M22 | MainViewModel.cs:427,453,468 | 子任务增删/勾选只刷新 CurrentTasks，FullWindow 的每日/截止集合展示过期数据 | 改调 LoadData() |
| M23 | TaskService.cs:76-77 | 每日完成记录完全不参与云同步，多设备连击/成就各算一套；MarkTaskDirty 只是无效重传 | 完成记录纳入同步协议 |
| M24 | StatisticsViewModel.cs:60,123-147 | StreakDays 从今天起算（白天未全清恒为 0，与连击显示矛盾）、依赖最近 7 天数据上限恒为 7 天、用当前任务数回溯历史 | 今天未全清从昨天起算；单独分页查询；口径与 ComboDays 统一 |

### UI 与基础设施（12 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M25 | MainWindow.xaml.cs:800-848 | _isDragging 只在 Drop 复位，ESC/列表外释放后拖拽永久失灵 | try/finally 包裹 DoDragDrop |
| M26 | CircularProgress.cs:14-24,109-127 | Pen 缓存只在 Progress 变化时失效，主题切换后圆环颜色不变（侧边栏迷你环与专注页均受影响） | 三个 DP 挂 PropertyChangedCallback 清缓存 |
| M27 | CircularProgress.cs:129-153 | Progress=1 时弧段起终点重合，进度环整个消失 | angle≥360 时画整圆 |
| M28 | MainWindow.xaml.cs:436-448 | 按钮 ⤢ 切换完整模式不走 ReRegisterHotkeys，全局热键全部静默死亡直到重启 | 收敛切换逻辑到 App 层统一路径 |
| M29 | HotkeyService.cs:54-61 | 全局劫持 Ctrl+N/Ctrl+F（其他程序全程不可用）；注册失败无反馈，_isRegistered 盲目置 true | 改 Ctrl+Alt+N/F；暴露注册结果 |
| M30 | MainWindow.xaml.cs:112-119 | Deactivated 无条件收起，打开任何模态框/MessageBox 都把面板收走、锚点漂移 | 有 owned 活动窗口时跳过收起 |
| M31 | MainWindow.xaml.cs:545-561 | 展开兜底定时器不检查 _isCollapsed，与快速收起竞争产生"看得见却自认为收起"的幽灵面板 | Tick 先判 _isCollapsed；failSafe 存字段互斥 |
| M32 | MainWindow.xaml.cs:692-709 | 卡片展开状态存在可视元素上，集合一重建就折叠/悬空引用/串卡 | IsExpanded 放进数据模型，DataTrigger 渲染 |
| M33 | ThemeManager.cs:29,82-150 | "跟随系统"不监听系统事件纹丝不动；Priority*/Primary/Type* 6 个画刷键从不纳入主题替换 | 注册 SystemEvents；补齐两套主题值 |
| M34 | NotificationService.cs:127 | ClearNotifiedTask 全项目零调用——修改截止日期后提醒永不恢复 | 保存 Deadline 变化时调用清理 |
| M35 | FullWindow.xaml.cs:156,205 | 番茄钟每秒 Tick 触发 2 次 SQLite 同步查询（25 分钟 = 3000 次），UI 线程磁盘 IO | 统计缓存，Tick 只更新剩余时间 |
| M36 | SettingsWindow.cs:343-363 + LoginWindow.xaml.cs:174-240 | 导入 JSON 成功后主界面不刷新（需重启可见）；注册/忘记密码按钮异步期间不禁用可并发提交 | 导入后调 SharedViewModel.LoadData()；统一 SetBusy |

---

## 五、轻微问题（P2，共 30 项）

| # | 位置 | 问题 |
|---|---|---|
| L1 | DatabaseService.cs:485,509-512 | CreatedAt 解析失败静默回退 DateTime.Now，无声篡改数据 |
| L2 | DatabaseService.cs:14-37 | 双检锁缺 volatile；public Initialize() 可绕过锁形成竞态 |
| L3 | DatabaseService.cs:342,908 | LastSyncedAt 格式混用可使 PurgeDeletedTasks 字符串比较失效（漏删积压） |
| L4 | MessageService.cs:41-47 | ShowConfirmation 同步封送有死锁窗口；NullMessageService 对破坏性操作默认"确认" |
| L5 | Database/init.sql:10,62-92 | user_id 可空产生 RLS 下不可见的幽灵行；策略脚本不幂等 |
| L6 | LevelService.cs:126-128 | BestComboDays 同步逻辑藏在 if(leveledUp) 内，实为死代码 |
| L7 | 全项目 10+ 处 | 日期 ToString 未用 InvariantCulture，非格里高利历区域下年份错乱 |
| L8 | PomodoroService.cs:133,116 | Break 状态既不能 Stop 也不能 Pause，只能干等休息结束 |
| L9 | App.xaml.cs:214-224 | OnExit 不处理进行中的番茄会话，已专注时间无记录无恢复 |
| L10 | AchievementService.cs:44-46 | 单枚徽章写库异常中断整轮检查且被吞掉，后续徽章漏检 |
| L11 | AchievementService.cs:76-77 | early_bird/night_owl 每次全表扫描逐行解析时间，流水多后有开销 |
| L12 | MainViewModel.cs:250-258 | LoadData 在 UI 线程同步执行 8+ 次全表查询，操作可能卡顿 |
| L13 | Models/TaskItem.cs:160-176 | DeadlineUrgency 依赖 DateTime.Now 但从不触发通知，"3小时后"文本停滞 |
| L14 | FeatureFlagService.cs:6-45 | 注释承诺与实现相反（Pro 全放行）；_licenseService 注入后从未使用 |
| L15 | LicenseService.cs:11-60 | Trial 仅存内存重启即重置；ValidateLicense Pro 分支不可达（骨架遗留） |
| L16 | SyncLogServiceTests.cs:9-14 | 测试写真实 %APPDATA% 覆盖用户 sync_log.json；依赖进程级单例 |
| L17 | MainViewModel.cs:321-349 | 添加任务后不刷新统计，统计页数字滞后 |
| L18 | StatisticsWindow.cs:143-182 | 「刷新」只重载绑定数字，成长曲线与番茄卡片保持陈旧快照 |
| L19 | FullWindow.xaml.cs:368-398 | 新增任务动画因容器尚未生成基本不生效 |
| L20 | NotificationService.cs:61-64,193-195 | Stop() 漏停 _midnightTimer；多条通知同位置完全重叠 |
| L21 | TaskDetailDialog.xaml.cs:36-47,162-170 | 打开对话框即置 _hasChanges=true（多余 DB 写）；截止日期可改到过去无校验 |
| L22 | LoginWindow.xaml.cs:252-268 | 诊断信息泄露 AnonKey 前 24 字符到 UI 与日志文件；登录窗无关闭按钮 |
| L23 | MainWindow.xaml.cs:124-154 | 时间栏显示到秒却每 30 秒刷新，秒数长期停滞 |
| L24 | MainWindow.xaml:238-242 | QuickDeadlinePicker 无主题适配，深色下亮白底刺眼 |
| L25 | AnimationService/TagControl/BlurHelper | 三处基础设施死代码（含高 DPI 发虚、Acrylic 与分层窗口不兼容两处隐患） |
| L26 | MainViewModel.cs:107-108 | v4.2.2 已移除模板页，模板服务死代码仍在启动时读文件 I/O；SaveTaskOrderCommand 零调用 |
| L27 | SyncService.cs:585-607 | Stop() 立即 Dispose CTS 且复位防重入标记，不等在途同步，快速登出→登录可并发双同步 |
| L28 | SyncViewModel.cs:91,121 | 手动上传/下载绕过防重入守卫，可与自动同步并发双写 |
| L29 | NetworkMonitor.cs:51-53 | UpdateStatus check-then-set 非原子，可能重复触发 ConnectivityChanged |
| L30 | SyncService.cs:423,435 | conflict 日志明文记录任务标题，导出 sync_log.json 时泄露隐私；Errors 字段从未赋值 |

---

## 六、功能优化建议（合并去重后 Top 10）

1. **XP 经济模型重构**（对应 S10/M14/M16/M17/M21）：XpLog 建 `(Source, COALESCE(TaskId,-1), Date)` 唯一索引，Reward 改为"INSERT OR IGNORE 幂等流水 → 影响行数才加 XP"的单事务方法，一次性消除 4 个重复发放/绕过路径；补 Uncomplete 回收。
2. **同步协议服务端权威化**（对应 S5/S7/S8/M10/M12）：updated_at 由数据库 trigger 维护、增量游标取服务器返回值、游标按用户隔离、下载分页；给 TaskItem 增加 LocalUpdatedAt 实现真 LWW + 远程胜前本地快照可恢复。
3. **会话状态机健壮化**（对应 S4/M7/M8/M9）：订阅时机修正 + 错误分类（网络类保留凭据重试，凭据类才登出）+ tmp 文件随机名加锁；监听 SignedOut 事件替代手工顺序依赖。
4. **贴边逻辑状态机重构**（对应 M25/M30/M31）：七个纠缠的状态标志合并为 `SidebarState { Expanded, Collapsing, Collapsed, Expanding }` + 动画完成回调驱动迁移，顺带删除 300ms 点击冷却补丁。
5. **数据刷新架构统一**（对应 M22/L12/L17）：单一 `RefreshAsync`（后台线程查询 + 单次 Diff 回投 UI）替代 5 个 Load* + 统计重查，顺带修复跨窗口数据过期。
6. **窗口生命周期基类**（对应 S15/L20）：集中"订阅/退订配对 + OnClosed 清理"，窗口切换复用实例。
7. **备份体系增强**（对应 S3）：CreateBackup 纳入软删任务与 DailyTaskCompletion/XpLog/PomodoroSession（游戏化数据目前完全不在备份内）；恢复成功前自动即时备份；恢复时维护旧→新 Id 映射。
8. **主题系统补全**（对应 S17/M26/M33）：SystemEvents 监听 + 补齐 6 个画刷键 + 转换器去硬编码 + CircularProgress 缓存失效回调。
9. **IClock 时间抽象**（对应 S9/L7）：注入时钟后午夜边界、跨天、时区场景全部可测，配合连击补结算落地。
10. **死代码清理**（对应 L25/L26/L14）：模板服务、AnimationService 六个零调用方法、TagControl、BlurHelper、SaveTaskOrderCommand、ExportService 未用的 _taskService 字段。

---

## 七、测试覆盖评估

现有 8 个测试文件覆盖了成长系统主干数学（升级循环、DeriveFromTotal、防重基础路径），但以下关键路径**零测试**：

- `SettleCombo` 断连清零分支；错过午夜的补结算（尚不存在，S9）
- `pomodoro_daily` / `pomodoro_round` 奖励逻辑（补上即暴露 S10）
- 绑定任务多次番茄的 XP 行为（M17）；暂停后完成的时长统计（M15）
- 一次跨多级的 Reward 事件与称号；挑战种子跨进程稳定性（M18）
- 成就 20 枚中仅 task_1 有行为测试；deadline_on_time 挑战推进
- 统计口径（streak 边界：当天未完成、恰好 7 天、任务数变化——M24）
- 同步冲突解决（S7 场景完全无测试）

**测试基建缺陷**：4 个测试类靠环境变量 `TODOSIDEBAR_TEST_DB` 指定临时库，但 `DatabaseService.Instance` 单例可能已被先执行的类初始化，环境变量失效，实际共享同一临时库（注释已承认并用相对断言规避）。建议 DatabaseService 支持实例注入。

---

## 八、审查确认无问题的方面

- **SQL 注入**：全部参数化，SearchTasks 的 LIKE 转义、动态列名来自硬编码字典，未发现注入面
- **凭据保护**：「记住我」密码经 DPAPI(CurrentUser+熵) 加密入库，解密失败返回 null 不回显；Supabase 凭据不进仓库（env/AppData 配置 fail-fast）
- **RLS 基础安全**：`user_id IS NULL` 的行对匿名与越权均不可读写（只会产生孤儿行不会泄露）
- **INPC 与线程**：三个 ViewModel 的 `[ObservableProperty]` 正确；所有 ObservableCollection 变更路径均在 UI 线程；番茄钟/挑战服务基于 DispatcherTimer
- **async void 卫生**：全项目 6 处 async void 均有 try/catch 保护（含委托给受守卫的 async Task 方法的模式）
- **日期键格式**：天键全局一致使用本地 `yyyy-MM-dd`，时间戳 UTC 存储，无混用（Tasks 时间列除外，见 M2）
- **午夜定时器**：间隔下限 1 秒保护、触发后自重排，无芝诺悖论类问题
- **软删墓碑链路**：DeleteTask → IsDirty=1 → 随上传同步 → 30 天 Purge，主链路正确（孤儿完成记录问题另列 M20）
- **ShowDialog Owner**：所有对话框调用方均正确设置 Owner
- **XAML 资源引用**：MainWindow 引用的 StaticResource 键全部存在，绑定路径逐一核对均可解析

---

## 九、修复路线图建议

**第一批（数据安全，建议立即）**：S1 删库阈值、S2 单例/Dispose、S3 备份恢复映射、S6 登出清库、S7 LWW 基线、S8 游标隔离
**第二批（同步可用性）**：S4 同步启动时机、S5 user_profile 幂等、M7-M9 会话健壮化、M11 Guid.TryParse
**第三批（功能正确性）**：S9-S14（连击补结算、番茄奖励、拖拽排序、统计口径、子任务两个交互 bug）、M20-M24
**第四批（体验与健壮）**：S15-S17、M25-M36
**第五批（还债）**：轻微项 + 死代码清理 + 测试缺口补齐

---

## 附录：审查方法说明

- 6 个模块审查各自逐行通读指定文件并与调用方交叉验证；报告中的行号基于 commit `cb11a7c` 工作区逐行读取
- 高危发现（S1/S4/S7/S10/S11/S13/S14/S16 及 user_profile 重复行）由主审逐一打开源码二次复核确认
- 三处依赖 Supabase 服务端/库版本行为的结论已标注"待验证"（M12 的 db-max-rows、M10 的序列化偏移、S5 的 Upsert 按主键解析语义）
- 编译验证：Debug 配置 0 错误通过
