# TodoSidebar 账号升级机制设计方案

> 主题名（暂定）：**「冒险者之旅」** —— 把每日任务变成冒险，把番茄钟变成修炼。
> 版本基线：v4.1.0 修复版 ｜ 设计日期：2026-08-04

---

## 一、设计理念

让用户**为了"变强"而回来**，而不是为了"打卡"而坚持。

- **任务 = 冒险委托**：完成任务是打怪升级；每天清空所有每日任务 = 完成一轮"副本挑战"。
- **番茄钟 = 修炼/专注充能**：25 分钟专注是"经验加速器"，绑定任务的番茄能产出双倍经验。
- **等级 = 冒险者等级**：Lv.1 → Lv.99，每升一级有称号、特效、进度条"打怪升级感"。
- **防刷原则**：所有经验必须来自**真实完成**的行为，中断的番茄不计分、重复操作不重复给分。

一句话核心循环：

> 完成任务 / 完成番茄 → 获得经验(XP) → 升级 + 解锁称号/徽章 → 触发奖励与特效 → 更有动力完成下一个任务

---

## 二、核心循环总览

```
┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐
│ 完成任务    │ → │ 获得 XP    │ → │ 经验条增长  │ → │ 升级！      │ → │ 称号/徽章/ │
│ 完成番茄钟  │   │ (+连击奖励) │   │ (进度动画)  │   │ (粒子特效)  │   │ 每日挑战   │
└────────────┘   └────────────┘   └────────────┘   └────────────┘   └────────────┘
                                                                          │
  激励下一次坚持 ←────────────────────────────── 解锁新目标 / 特殊体验 ←──────┘
```

---

## 三、经验值（XP）体系

### 3.1 经验来源表

| 行为 | 基础 XP | 说明 |
|------|---------|------|
| 完成 1 个每日任务 | +10 | 基础奖励 |
| 完成 1 个截止任务 | +15 | 挑战更高 |
| 截止日前按时完成 | +5 | 守时奖励 |
| 高/中优先级加成 | +5 / +3 | 难度加成（低优先级无加成） |
| 连续完成每日任务（连击） | 连击天数 × 2 | 第 2 天 +4，第 7 天 +14… |
| 完成 1 个番茄钟（25 分钟） | +5 | 基础专注奖励 |
| 完成绑定任务的番茄钟 | +10 | 任务+专注联动，鼓励绑定 |
| 完成一轮 4 个番茄 | +15 | 整轮奖励 |
| 达成每日专注目标（如 4 番茄） | +10 | 每日目标 |
| 里程碑创建/达成 | +20 | 里程碑事件 |
| 使用模板创建任务 | +2 | 轻度引导奖励 |
| 完成每日挑战（见 5.4） | +20/个 | 每日随机挑战 |

### 3.2 升级公式（温和平滑型）

```
升级所需 XP = 100 + (当前等级 - 1) × 20
```

- Lv.1→2 需 100 XP；Lv.10→11 需 280；Lv.30→31 需 680；Lv.50→51 需 1080。
- 按日均 60~100 XP 估算：约 1 周升 3~4 级（前期升级快、爽感足），Lv.30 约 2~3 个月，Lv.50 约 6 个月——**前期升级节奏快，后期靠坚持**，符合游戏化常规曲线。

### 3.3 升级体验（"升级感"的关键）

| 触发点 | 表现 |
|--------|------|
| 经验条 | 侧边栏/完整模式顶部常驻"等级 + 经验条"，经验增长有平滑动画（复用 AnimationService 数字动画） |
| 升级瞬间 | 全屏 300ms 粒子爆炸（复用 `AnimationService.CreateCompletionParticles`）+ 等级数字跳动 |
| 升级横幅 | 顶部滑出横幅："Lv.5 → 勤奋修行者"，持续 3 秒可点击展开成就面板 |
| 特殊等级 | Lv.10/20/30/50/70/99 触发专属主题特效（侧边栏主题色渐变、称号角标发光） |

---

## 四、番茄钟深度集成（核心卖点）

### 4.1 番茄钟本体

- 标准节奏：**25 分钟专注 + 5 分钟短休息**，4 个番茄后 **15 分钟长休息**（设置可调）。
- 三种使用方式：
  1. **快速番茄**：不绑定任务，纯专注计时（适合临时专注）；
  2. **任务番茄**：选择要专注的任务，完成番茄后任务累计 `ActualMinutes += 25`，达到 `EstimatedMinutes` 时弹窗提示"可以收尾完成啦"；
  3. **专注排队**：把当前任务列表拖入专注队列，逐个番茄专注。
- **中断处理**：中途主动停止 → 记录"中断"**不计 XP**；支持暂停（3 分钟内小中断可恢复，暂停累计上限 5 分钟）。
- **结束反馈**：番茄完成时右下角通知（复用 NotificationWindow）+ 小粒子 + 顶部横幅 "修炼完成 +10 XP"。

### 4.2 番茄钟入口（UI 落点）

| 位置 | 形式 |
|------|------|
| 侧边栏模式（MainWindow） | 底部新增圆形番茄进度环（复用 `CircularProgress` 控件），点击展开迷你计时器面板 |
| 完整模式（FullWindow） | 新增「专注」标签页 `TabFocus`，内置大号番茄环 + 任务绑定下拉 + 今日专注统计 |
| 全屏沉浸模式（可选 V2） | 专注时一键全屏 + 毛玻璃，进入"勿扰状态" |
| 统计窗口 | 新增"专注统计"卡片：今日番茄数、今日专注时长、本周专注热力图 |

### 4.3 番茄与升级的联动

- 每个完成番茄：+5 XP（基础）+ 绑定任务 +5 = **最高 +10/番茄**；
- 连续完成 4 个番茄（整轮）：额外 +15，番茄环出现"4 连"火焰角标；
- 每日专注目标（默认 4 个番茄）达成：额外 +10，并点亮"今日修炼完成"徽章；
- **番茄果实收集**（娱乐化扩展）：每个完成番茄 = 1 个"专注果实"，可查看"果实仓库"——集齐 25/50/100 颗解锁特殊称号（如"番茄种植大师"）。

---

## 五、娱乐感设计

### 5.1 连击系统（Combo / Streak）

- **规则**：当天完成**全部**每日任务 → 连击 +1 天；断一天连击清零（展示"差一天就 7 连"的挽留文案）。
- **奖励**：连击天数直接进 XP 加成（见 3.1），并解锁连击徽章：7 天「七日之约」、30 天「月度冠军」、100 天「百日筑基」。
- **展示**：侧边栏顶部显示 🔥 x N 连击火焰标（数字 + 火焰 emoji），越久越亮。

### 5.2 成就徽章系统（30+ 枚，分类解锁）

| 分类 | 示例徽章 |
|------|----------|
| 起步（5） | 新手上路（完成第 1 个任务）、初尝专注（第 1 个番茄）、迈出第一步（第 1 个里程碑） |
| 坚持（6） | 七日之约、月度冠军、百日筑基、早鸟（6 点前完成任务）、夜猫子（23 点后完成）、周全勤 |
| 专注（8） | 番茄入门（10 个）、番茄达人（50 个）、番茄大师（100 个）、全神贯注（单日 8 番茄）、四连修炼、专注百分钟 |
| 挑战（6） | 百炼成钢（100 任务）、守时者（50 次按时）、多面手（单日 3 类任务全清）、里程碑缔造者、模板大师、子任务终结者 |
| 隐藏彩蛋（5+） | 凌晨 0-2 点完成（夜行侠）、同一天完成 5 个高优先级（火力全开）… |

- 解锁时机：**解锁瞬间弹窗 + 粒子**，徽章面板支持图鉴式浏览（已解锁彩色 / 未解锁灰色剪影 + 解锁条件提示）。

### 5.3 等级称号体系

| 等级 | 称号 | 解锁体验 |
|------|------|----------|
| Lv.1 | 初出茅庐 | 默认 |
| Lv.5 | 勤勉学徒 | 称号在侧边栏显示 |
| Lv.10 | 专注修行者 | 升级横幅特效 |
| Lv.20 | 时间掌控者 | 主题色微调（默认深紫 → 深青） |
| Lv.30 | 任务大师 | 称号角标发光 |
| Lv.50 | 效率贤者 | 专属头像框 |
| Lv.70 | 时间旅人 | 全窗渐变色主题 |
| Lv.99 | 传说冒险者 | 特殊"满级"成就 + 称号彩带 |

> 称号仅为**视觉荣誉**，不锁定功能（避免商业化/公平性问题）；Pro 功能仍由 License 体系控制。

### 5.4 每日挑战（3 个/天，随机生成）

```
例：
  🎯 完成 3 个每日任务（0/3）        → +20 XP
  🎯 完成 2 个番茄钟（0/2）          → +20 XP
  🎯 按时完成 1 个截止任务（0/1）    → +25 XP
```

- 每日零点由种子算法生成 3 个挑战（保证当天固定、次日刷新），完成实时打勾。
- 全完成额外 +30"挑战全清"奖励。

### 5.5 随机宝箱（V2 可选扩展）

- 当天清空全部每日任务后，出现"今日宝箱"按钮，开启随机获得：双倍经验卡（下次任务 XP ×2）、经验加成（+20 XP）、称号碎片（集齐 5 片兑换定制称号）。
- 属于锦上添花的付费/运营位，首版可只做"双倍经验卡"一种。

---

## 六、数据层设计

沿用现有 `MigrateDatabase` 列检查模式新增 4 张表（`Database/init.sql` 同步更新）：

```sql
-- 1. 用户成长档案（单行）
CREATE TABLE UserProfile (
    Id INTEGER PRIMARY KEY CHECK (Id = 1),
    Level INTEGER NOT NULL DEFAULT 1,
    Xp INTEGER NOT NULL DEFAULT 0,            -- 当前等级内经验
    TotalXp INTEGER NOT NULL DEFAULT 0,
    TotalPomodoros INTEGER NOT NULL DEFAULT 0,
    TotalFocusMinutes INTEGER NOT NULL DEFAULT 0,
    ComboDays INTEGER NOT NULL DEFAULT 0,     -- 当前连击天数
    BestComboDays INTEGER NOT NULL DEFAULT 0,
    Title TEXT NOT NULL DEFAULT '初出茅庐',
    LastXpDate TEXT                           -- 上次获得 XP 日期（防重复日奖励）
);

-- 2. XP 流水（可追溯 + 可同步 + 防刷）
CREATE TABLE XpLog (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    Source TEXT NOT NULL,                     -- task_complete / pomodoro / combo / challenge / milestone ...
    Amount INTEGER NOT NULL,
    TaskId INTEGER,
    Date TEXT NOT NULL,
    CreatedAt TEXT NOT NULL
);

-- 3. 成就解锁记录
CREATE TABLE AchievementUnlocks (
    AchievementId TEXT PRIMARY KEY,
    UnlockedAt TEXT NOT NULL
);

-- 4. 番茄会话
CREATE TABLE PomodoroSession (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    TaskId INTEGER,                           -- 绑定任务（可空）
    StartTime TEXT NOT NULL,
    EndTime TEXT,
    DurationMinutes INTEGER NOT NULL,
    Completed INTEGER NOT NULL DEFAULT 0,     -- 1=完成 0=中断
    Date TEXT NOT NULL
);

-- 5. 每日挑战（Date+ChallengeId 唯一）
CREATE TABLE DailyChallenge (
    Date TEXT NOT NULL,
    ChallengeId TEXT NOT NULL,
    Progress INTEGER NOT NULL DEFAULT 0,
    Target INTEGER NOT NULL,
    Completed INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (Date, ChallengeId)
);
```

**服务层规划**：
- `Services/LevelService.cs`：等级/XP 计算、升级判定、XP 记账（写 XpLog + UserProfile，同一事务）；
- `Services/PomodoroService.cs`：番茄钟状态机（专注/休息/暂停/中断）、会话持久化、结束回写任务 ActualMinutes；
- `Services/AchievementService.cs`：徽章条件检测 + 解锁记录 + 通知事件；
- `Services/DailyChallengeService.cs`：种子生成挑战、进度更新。

**云同步策略**：`XpLog`/`PomodoroSession` 增量同步（沿用 SyncService 的 `IsDirty` 模式，按 `Date` 维度冲突少）；`UserProfile` 以"总 XP 最大者胜"合并，避免跨设备等级回退。

---

## 七、与现有代码的集成点（已核验可复用）

| 现有能力 | 升级系统中的用途 |
|----------|------------------|
| `AnimationService.CreateCompletionParticles` | 升级/番茄完成/徽章解锁的粒子特效 |
| `CircularProgress` 控件（刚修复 AffectsRender） | 番茄钟进度环、等级经验环 |
| `NotificationWindow`（右小角通知） | 番茄结束、升级横幅、挑战达成提醒 |
| `StatisticsWindow` + `StatisticsViewModel` | 新增"专注统计"卡片与等级曲线图 |
| `DatabaseService` 迁移模式 | 新表建表/加列 |
| `SyncService` / `XpLog.IsDirty` | 成长数据云同步 |
| `SettingsWindow` | 番茄钟时长/每日目标设置 |
| `MainViewModel` 午夜定时器 | 每日零点：连击结算、挑战刷新、每日目标重置 |
| `TaskItem.EstimatedMinutes/ActualMinutes` | 任务番茄绑定：专注时长写入、收尾提示 |

---

## 八、防刷与数据一致性

1. **真实行为才给分**：XP 写入统一走 `LevelService.Reward(source, amount, taskId)`，内部用事务 + `LastXpDate` 防止同源同日重复结算。
2. **番茄诚实计时**：中断不计 XP；暂停有上限；结束时间异常（时长 < 50% 设定值）按中断处理。
3. **连击由午夜定时器结算**：零点检查"昨日每日任务是否全清"决定连击 +1 或清零，只在结算点写库。
4. **本地优先 + 云同步**：离线升级不阻塞；`XpLog` 可追溯，合并冲突时取更高 TotalXp。

---

## 九、分阶段实施计划

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| **P1 数据与核心** | 4 张新表 + `LevelService`（XP/升级/记账）+ 任务完成打点 | 任务完成有 XP、经验条显示、升级横幅与粒子 |
| **P2 番茄钟** | `PomodoroService` + 侧边栏迷你番茄 + 完整模式「专注」页 + 番茄统计 | 番茄完成计 XP、绑定任务时长回写、专注统计卡片 |
| **P3 娱乐化** | 连击系统 + 成就徽章（首批 20 枚）+ 徽章图鉴面板 | 连击火焰标、解锁弹窗、徽章图鉴 |
| **P4 每日挑战与进阶** | `DailyChallengeService` + 挑战面板 + 等级称号体系（含特殊等级特效） | 每日 3 挑战、称号系统全量上线 |
| **P5 打磨与同步** | XpLog/番茄会话云同步 + 等级曲线统计图 + 彩蛋徽章 + V2 可选（宝箱/果实仓库） | 全量功能 + 数据同步 |

> 预估 P1~P3 为核心体验闭环（约 60% 工程量），P4~P5 为内容扩充。每阶段独立可交付、可回退。

---

## 十、风险与取舍

- **功能锁定顾虑**：等级/称号纯视觉荣誉，不锁定任何功能 → 避免"未升级不能用功能"的反感，也规避商业化合规问题。
- **刷分风险**：见第八节防刷设计，重点卡住"番茄中断"与"同日重复结算"两个口子。
- **数据量**：XpLog 增量增长可控（每条 <100B，一年约 2~3 万条，SQLite 无压力），定期清理 1 年前旧流水。
- **与现有统计的一致性**：`StatisticsViewModel` 新增专注维度时复用现有查询模式，不侵入既有统计逻辑。

---

*本方案为设计稿，确认方向后可进入 P1 实施。*

---

## 十一、实施进度

### ✅ P1 已完成（2026-08-04）

| 模块 | 状态 | 说明 |
|------|------|------|
| 数据层 | ✅ | 5 张新表（UserProfile/XpLog/AchievementUnlocks/PomodoroSession/DailyChallenge）建表完成；`DatabaseService` 新增 GetUserGrowth/SaveUserGrowth/HasXpLog/AddXpLog/GetTodayXp，全部走锁；支持 `TODOSIDEBAR_TEST_DB` 环境变量隔离测试库 |
| 模型 | ✅ | `Models/UserGrowth.cs`：UserGrowth / LevelInfo / LevelUpEventArgs |
| LevelService | ✅ | `Services/LevelService.cs`：升级公式 `100+(Lv-1)*20`、称号映射、Reward 记账（防重：同源+同任务+同日不重复结算）、升级事件 |
| 任务打点 | ✅ | `TaskService.CompleteTask` 完成后发 XP：每日+10/截止+15、按时+5、高优先级+5/中+3 |
| 经验条 UI | ✅ | MainWindow 顶栏 + FullWindow 顶栏新增等级条（等级+称号+经验进度条） |
| 升级反馈 | ✅ | 升级瞬间：横幅滑入（"升级！Lv.X 新称号"）+ 3 次粒子爆炸，3 秒后淡出 |
| 验证 | ✅ | Release 0 警告 0 错误；37/37 单元测试通过（新增 5 个 LevelService 测试） |

### ✅ P2 番茄钟已完成（2026-08-04）

| 模块 | 状态 | 说明 |
|------|------|------|
| PomodoroService | ✅ | `Services/PomodoroService.cs`：状态机（Idle/Focus/Paused/Break）、25/5/15 分钟节奏、4 轮长休息、完成写会话+回写任务 ActualMinutes、XP 联动（未绑定+5/绑定+10、第 4 个整轮+15、每日目标+10）、中断不计 XP、休息归零安全回待机 |
| 数据层 | ✅ | `DatabaseService` 番茄会话读写（GetPomodoroCountByDate / GetFocusMinutesByDate / AddPomodoroSession，全部加锁） |
| 侧边栏迷你番茄 | ✅ | MainWindow 底部迷你番茄条：进度环（复用 CircularProgress）+ 倒计时 + 状态文本 + 开始/暂停/停止（中断需确认） |
| 完整模式「专注」页 | ✅ | FullWindow 新增 🍅 专注 Tab：大番茄环 + 倒计时 + 任务绑定下拉 + 控制按钮 + 今日番茄/本轮/每日目标统计 |
| 专注统计 | ✅ | StatisticsWindow 新增「专注统计」卡片（今日番茄/专注分钟/中断） |
| 验证 | ✅ | 主项目 Release 0 警告 0 错误；48/48 单元测试通过（含 6 个番茄钟 + 5 个连击/成就新测试；此前 P2 测试因环境 testhost 锁未跑成，本次环境恢复后全量通过） |

### ✅ P3 娱乐化已完成（2026-08-04）

| 模块 | 状态 | 说明 |
|------|------|------|
| 连击系统 | ✅ | `LevelService.SettleCombo()` 午夜结算：昨天全清每日任务 → 连击+1 并发放连击经验（天数×2）；断连清零；当天已结算则幂等跳过；`ComboSettled` 事件 |
| 成就服务 | ✅ | `Services/AchievementService.cs`：首批 20 枚徽章（任务 5 / 专注 6 / 连击 4 / 单日与等级 3 / 彩蛋 2），条件判定 + 幂等解锁 + `AchievementUnlocked` 事件 |
| 检查点 | ✅ | 任务完成、番茄完成、午夜结算、启动补检四处触发 `CheckAll()` |
| 连击展示 | ✅ | 侧边栏 + 完整模式等级条显示 `🔥 xN`（橙色） |
| 成就图鉴 | ✅ | 新增 `AchievementWindow`：20 枚徽章全部展示，已解锁彩色 + ✅，未解锁灰色剪影 + 条件；顶部"🏅"按钮打开；解锁横幅 + 粒子（复用升级横幅） |
| 验证 | ✅ | Release 0 警告 0 错误；48/48 测试通过（新增 2 连击 + 3 成就测试） |

### ⏳ 待实施

### ✅ P4 每日挑战已完成（2026-08-04）

| 模块 | 状态 | 说明 |
|------|------|------|
| DailyChallengeService | ✅ | `Services/DailyChallengeService.cs`：5 种挑战模板池，按日期种子每日生成 3 个（当天固定、次日刷新）；`RegisterProgress` 前缀匹配推进进度，达标发 XP（+20~30）；防重：同类型同天只发一次 |
| 挑战类型 | ✅ | 完成每日任务 ×3 / ×5、完成番茄 ×2 / ×4、按时完成截止任务 ×1 |
| 进度触发 | ✅ | 任务完成 → 推进每日任务/按时挑战；番茄完成 → 推进专注挑战 |
| 挑战面板 | ✅ | FullWindow「每日」Tab 顶部新增「🎯 今日挑战」区：图标 + 标题 + 进度 x/y + 奖励 XP + 完成 ✅/进行中 ⏳；切换 Tab / 进度更新自动刷新 |
| 称号特效 | ✅ | `LevelService.FormatLevelDisplay`：特殊等级（Lv.10/20/30/50/70/99）称号追加 ✨ |
| 验证 | ✅ | Release 0 警告 0 错误；51/51 测试通过（新增 3 个挑战测试；测试项目改为串行执行 `DisableTestParallelization`，消除共享单例状态干扰） |

### ✅ P5 云同步 + 等级曲线图已完成（2026-08-04）—— 升级系统全流程收尾

| 模块 | 状态 | 说明 |
|------|------|------|
| 等级曲线图 | ✅ | StatisticsWindow 新增「📈 成长曲线」卡片：近 7 天每日经验折线（Canvas 自绘，无第三方依赖）；`DatabaseService.GetDailyXpLastDays` 补全零值日期 |
| 成长数据同步 | ✅ | `XpLog`/`PomodoroSession` 表加 `IsDirty` 列（迁移自动加）；新增流水即标记待同步；`SyncGrowthDataAsync` 随主同步自动执行 |
| Supabase 模型 | ✅ | `SyncXpLog` / `SyncPomodoroSession` / `SyncUserProfile`（对应 xp_log / pomodoro_session / user_profile 表） |
| 档案合并 | ✅ | 用户档案按累计总经验「大者胜」双向合并；`LevelService.DeriveFromTotal` 从 TotalXp 重算等级/级内经验 |
| 同步策略取舍 | ✅ | 流水以本机为准（只上传不同步下载，跨设备 TaskId 不对应）；跨设备等级一致性由 TotalXp 兜底 |
| 验证 | ✅ | Release 0 警告 0 错误；53/53 测试通过（新增 DeriveFromTotal / 每日 XP 曲线数据测试） |

### 🏁 全部阶段完成（P1 ~ P5）

升级系统五大阶段全部实施完毕：等级/经验/升级（P1）→ 番茄钟（P2）→ 连击/成就/图鉴（P3）→ 每日挑战/称号特效（P4）→ 云同步/等级曲线（P5）。
**累计：新增 10 个文件、修改 20+ 文件；Release 0 警告 0 错误；53/53 单元测试通过。**

> ⚠️ **云同步启用前提**：需在 Supabase 控制台创建 `xp_log` / `pomodoro_session` / `user_profile` 三张表并配置 RLS；同步为尽力而为，表缺失时仅记录日志不影响本地功能。另建议轮换已泄露的旧 Anon Key。
